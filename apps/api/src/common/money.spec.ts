import{money,multiplyMoney,percentage}from'./money';describe('money arithmetic',()=>{it('uses decimal multiplication instead of binary floating point',()=>{expect(multiplyMoney('0.10',3).toString()).toBe('0.3')});it('rounds percentages half-up at four decimal places',()=>{expect(percentage('19.99','8.25').toFixed(4)).toBe('1.6492')});it('preserves large fixed values',()=>{expect(money('999999999999.9999').toFixed(4)).toBe('999999999999.9999')})});
