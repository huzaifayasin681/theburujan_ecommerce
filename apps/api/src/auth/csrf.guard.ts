import{CanActivate,ExecutionContext,ForbiddenException,Injectable}from'@nestjs/common';import{Reflector}from'@nestjs/core';import{timingSafeEqual}from'node:crypto';import type{Request}from'express';import{IS_PUBLIC_KEY}from'./auth.decorators';@Injectable()export class CsrfGuard implements CanActivate{constructor(private readonly reflector:Reflector){}canActivate(context:ExecutionContext){if(this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY,[context.getHandler(),context.getClass()]))return true;const request=context.switchToHttp().getRequest<Request>();if(['GET','HEAD','OPTIONS'].includes(request.method)||!request.cookies?.access_token)return true;const cookie=String(request.cookies.csrf_token??'');const header=request.get('x-csrf-token')??'';const a=Buffer.from(cookie);const b=Buffer.from(header);if(!cookie||a.length!==b.length||!timingSafeEqual(a,b))throw new ForbiddenException({code:'CSRF_INVALID',message:'Request security token is missing or invalid'});return true}}
